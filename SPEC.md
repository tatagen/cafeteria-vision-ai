# 食堂混雑解析システム (SmartCafé Intelligence) 仕様書

## 1. 概要
本システムは、食堂の定点カメラ映像をAIで解析し、混雑状況をリアルタイムに把握・共有するためのWebアプリケーションです。

## 2. 画面構成
### 2.1 表示画面 (Public View)
- 食堂利用者向けの閲覧専用画面。
- 「混雑度」「人数」「占有率」「行列状況」を視覚的に分かりやすく表示。
- 白を基調としたモダンでクリーンなデザイン。

### 2.2 管理画面 (Admin View)
- 管理者専用の制御・設定画面。
- **認証:** IDとパスワードによるログイン。
- **カメラ制御:** ライブフィードの確認、ON/OFF切り替え。
- **解析モード:**
  - **自動:** 90秒ごとに自動解析を実行。
  - **手動:** カウントや混雑度を管理者が自由に変更可能。
- **即時解析:** 任意のタイミングで今すぐAIスキャンを実行。

### 2.3 ダッシュボード (Analytics)
- 過去の解析データの可視化。
- 利用人数・占有率の時系列推移グラフ。
- データのCSVダウンロード機能。

## 3. 解析ロジック
- **エンジン:** Gemini 1.5 Flash。
- **入力:** カメラからのスナップショット（Base64画像）。
- **出力要素:**
  1. 人数（整数）
  2. 混雑度（空席あり / やや混雑 / 混雑 / 満席）
  3. 占有率（0-100%）
  4. 行列の有無（Boolean）
  5. AIによる分析理由

## 4. 動作環境・制約
- **デバイス:** カメラ搭載のPC、タブレット、スマートフォン。
- **ブラウザ:** カメラアクセス権限が許可されているモダンブラウザ（Chrome, Safari等）。
- **データ永続化:** 履歴データはブラウザの `localStorage` に保存（デバイス間での共有には別途DBが必要）。
- **同時ログイン:** 各デバイスが独立してカメラを起動可能。解析も各ブラウザで独立して動作。
